<header id="header" class="header" ng-controller="HeaderController">
    <nav class="navbar" role="navigation">
        <div class="container-fluid" ng-cloak>
            <div class="navbar-header">
                <a href="#" class="pull-right" ng-click="login($event)" ng-show="showLogin() && !auth">
                    <span class="pull-left icon icon-enter" style="margin-top:5px;"></span>
                    <span class="pull-left hidden-xs" style="padding-left:5px;">Login</span>
                </a>
            </div>
        </div>
    </nav>
</header>

